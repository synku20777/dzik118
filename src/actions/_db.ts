// Re-exported under the name existing action files already import; the
// canonical implementation lives in src/lib/db-request.ts so pages can share
// it too.
export { withRequestDb as withDb } from "../lib/db-request";
